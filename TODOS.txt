FIXTURES:
-find better algo for city and country
-setting the possible service for each material

EVENT LISTENER
-event listenter to hash the password

DB:
- Give to employee data from the contract?

SECURITY:
- Token JWT for connection ('api/auth')

CRUD:
- Does 'User' need CRUD?
- Roles permissions for certain for CRUD
- API Platform CRUD
(#[ApiResource] https://ld-web.github.io/hb-sf-pe9-course/api/api-platform-installation?_highlight=cors/hb-sf-pe9-course/api/api-platform-installation#cors--authentification)
- #[Groups()] to show (or not) different info (Lucas courses)